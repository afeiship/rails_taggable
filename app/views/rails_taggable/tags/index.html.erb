<h1>Tags#index</h1>
<p>Find me in app/views/tags/index.html.erb</p>



<% @tags.each do |item| %>
    <p>
        <a href="/rails_taggable/tags/<%= item.id %>/edit"><%= item.name %></a>
        <%= link_to 'Delete', rails_taggable_tag_path(item), method: :delete, data: { confirm: 'Are you sure? This cannot be undone.' } %>
    </p>
<% end %>